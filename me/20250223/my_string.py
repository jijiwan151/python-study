# name = "JiWanPark"
# school = "PanGyo"
# print(name)
# print(school)
# print(name, "은 ", school, "을 다녀")
# print(name + "은 " + school + "HighSchool을 다녀")
# 응원 = "fighting "
# print(응원 + 응원 + 응원)
# print(응원 * 10)
gpt = "현재 실행하는 파일이 decimal.py이라는 이름을 갖고 있으면, Python이 내장 모듈 decimal 대신 이 파일을 불러오려고 해서 충돌이 발생해요해결방법:파일이름을 decimal.py가 아닌 다른이름으로 변경하세요.예를 들어:operator.py → my_operator.py\이렇게 변경한 후 다시 실행하면 오류가 사라질 거예요.현재 실행하는 파일이 decimal.py이라는 이름을 갖고 있으면, Python이 내장 모듈 decimal 대신 이 파일을 불러오려고 해서 충돌이 발생해요해결방법:파일이름을 decimal.py가 아닌 다른이름으로 변경하세요.예를 들어:operator.py → my_operator.py\이렇게 변경한 후 다시 실행하면 오류가 사라질 거예요.현재 실행하는 파일이 decimal.py이라는 이름을 갖고 있으면, Python이 내장 모듈 decimal 대신 이 파일을 불러오려고 해서 충돌이 발생해요해결방법:파일이름을 decimal.py가 아닌 다른이름으로 변경하세요.예를 들어:operator.py → my_operator.py\이렇게 변경한 후 다시 실행하면 오류가 사라질 거예요.현재 실행하는 파일이 decimal.py이라는 이름을 갖고 있으면, Python이 내장 모듈 decimal 대신 이 파일을 불러오려고 해서 충돌이 발생해요해결방법:파일이름을 decimal.py가 아닌 다른이름으로 변경하세요.예를 들어:operator.py → my_operator.py\이렇게 변경한 후 다시 실행하면 오류가 사라질 거예요.현재 실행하는 파일이 decimal.py이라는 이름을 갖고 있으면, Python이 내장 모듈 decimal 대신 이 파일을 불러오려고 해서 충돌이 발생해요해결방법:파일이름을 decimal.py가 아닌 다른이름으로 변경하세요.예를 들어:operator.py → my_operator.py\이렇게 변경한 후 다시 실행하면 오류가 사라질 거예요."
count = len(gpt)
print(count)
position = gpt.find("파일")
replaceGpt = gpt.replace("파일", "FILE")
# print(position)
print(replaceGpt)

# print(str(replaceGpt.count('FILE')) + " 번 사용되었어")


print(str(replaceGpt.count('file')) + "번 사용됨")